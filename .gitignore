node_modules/
dist/
# Production client bundle (vite build) — the deploy web root
/public/
*.tsbuildinfo
data/*.db
data/*.db-*
data/worldcache/
# Reel mezzanines and contact sheets — the tape a take leaves behind.
# Only the encoded delivery files under packages/client/public/reels/
# are ever committed; a raw 1080p mezzanine is 30 MB and reproducible.
/.reels-raw/

# content-addressed voice clip store — runtime data, deploy-persisted via shared/voice
data/voice/
.DS_Store
.playwright-mcp/

# Playwright audit screenshots land in the repo root — never ship them
/*.png
/*.jpeg
/.playwright-mcp/

# Voice takes (human + Chatterbox) stage here before import — audio never
# ships through git; the ledger (db + data/voice) is the system of record
/voicework/recorded/*
/voicework/generated/*
