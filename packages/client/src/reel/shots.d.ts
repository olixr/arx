import type { Shot } from './types.js';
/**
 * THE SLATE — every reel the front page can draw from.
 *
 * Written like storyboard cards, because that is what they are. Each
 * one names a place, a body, a foe and a camera, and the director
 * performs it against the live game.
 */
export declare const SHOTS: Shot[];
export declare function shotById(id: string): Shot | undefined;
//# sourceMappingURL=shots.d.ts.map